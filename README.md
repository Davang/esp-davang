# esp-davang

Custom esp davang development project. It contains custom componentes and support tools.
It has a export bash script as esp-idf to easily export the custom paths


## components

Submodule with custom davang components check that repository documentation.

---
Davang
